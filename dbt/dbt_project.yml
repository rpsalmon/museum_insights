name: 'museum_insights'
version: '1.0.0'
config-version: 2

profile: 'museum_insights'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  museum_insights:
    staging:
      +materialized: view
    marts:
      +materialized: table
